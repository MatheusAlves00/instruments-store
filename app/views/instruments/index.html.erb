<%= render 'layouts/navbar' %>

<div class="row gx-0 m-auto">
  <div class="col-2 p-0">
    <div class="list-group" id="list-tab" role="tablist">
      <a class="list-group-item list-group-item-primary">Instrument types</a>
      <a class="list-group-item list-group-item-action active" data-toggle="list" href="#list-all" role="tab" aria-controls="home">All</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-guitar" role="tab" aria-controls="profile">Guitar</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-electric-guitar" role="tab" aria-controls="profile">Electric Guitar</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-bass" role="tab" aria-controls="messages">Bass</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-keyboard" role="tab" aria-controls="settings">Keyboard</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-piano" role="tab" aria-controls="settings">Piano</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-flute" role="tab" aria-controls="settings">Flute</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-cello" role="tab" aria-controls="settings">Cello</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-clarinet" role="tab" aria-controls="settings">Clarinet</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-accordion" role="tab" aria-controls="settings">Accordion</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-saxophone" role="tab" aria-controls="settings">Saxophone</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-violin" role="tab" aria-controls="settings">Violin</a>
      <a class="list-group-item list-group-item-action" data-toggle="list" href="#list-drums" role="tab" aria-controls="settings">Drums</a>
    </div>
  </div>

  <div class="col-10 p-0 m-0">
    <div class="tab-content" id="nav-tabContent">

      <div class="tab-pane fade show active" id="list-all" role="tabpanel" aria-labelledby="list-home-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, '') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-guitar" role="tabpanel" aria-labelledby="list-profile-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Guitar') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-electric-guitar" role="tabpanel" aria-labelledby="list-profile-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Electric guitar') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-bass" role="tabpanel" aria-labelledby="list-messages-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Bass') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-keyboard" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Keyboard') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-piano" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Piano') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-flute" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Flute') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-cello" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Cello') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-clarinet" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Clarinet') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-accordion" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Accordion') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-saxophone" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Saxophone') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-violin" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Violin') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

      <div class="tab-pane fade" id="list-drums" role="tabpanel" aria-labelledby="list-settings-list">
        <section class="py-5">
          <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

              <% @instruments.each do |instrument| %>
                <%= show_instrument(instrument, instrument.name, instrument.model, instrument.brand, instrument.price, instrument.photo, 'Drums') %>
              <% end %>

            </div>
          </div>
        </section>
      </div>

    </div>
  </div>
</div>

<%= render 'layouts/footer' %>
